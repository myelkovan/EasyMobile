import Foundation
import UIKit


class #NAME#: MYUITableViewController{
	#IBOUTLET_LIST#
	var dataobject = #DATA_MODEL#()

       

	override func viewDidLoad() {
		super.viewDidLoad()
		self.d_object = dataobject
		dataobject.of_retrieve()
	}

   


	#SEARCH#
	override func of_filterForSearch(_ searchText: String)->Int {
		dataobject.data = dataobject.originaldata.filter({ (row: #DATA_MODEL#.str_data) -> Bool in
			return (row.#SERACH_FIELD#.lowercased() as NSString).contains(searchText.lowercased())
			})
		return dataobject.data.count
	}
	#ENDSEARCH#

	

	/* Most used function for tableview. If you don't use delete them from here or from template
	override func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        
		switch dataobject.data[indexPath.row].?  {
		}
		self.performSegue(withIdentifier: "x", sender: self)
  	}
	
    
	override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
		if segue.identifier == "toMessageDetails"{
 			let target_view = segue.destination as! 
			target_view.? = ?
		}
 	}
	*/


}





  
